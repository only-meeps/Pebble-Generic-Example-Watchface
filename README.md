# Generic Pebble Watchface Templete

Just a simple watchface that you can use as a starter project. Tested on all pebble platforms (including gabbro). Uses default system fonts, so no import is required.
